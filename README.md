# WC3WeldAran

Перенос карты **War for WeldAran** (WFWA) из Warcraft III 1.26 в самостоятельную игру
на собственном движке.

Репозиторий — дом проекта: инструменты конвертации, извлечённые данные,
документация, код движка и клиент, в котором в карту уже можно сесть играть.

---

## Идея

Исходная карта — `.w3x` на 25.6 МБ, которая уже не помещается в возможности движка
Warcraft III 1.26. Она максимального размера (480×480 тайлов), с 25 тысячами
декораций и скриптом на 765 КБ. Дальше развивать её внутри WC3 некуда.

Ключевой вывод анализа: **скрипт карты использует всего 321 функцию движка**
(из ~1900 в `common.j`). Это значит, что задача — не «переписать игру с нуля», а
**построить рантайм, реализующий эти 321 функцию**. Игровая логика (16 177 строк
JASS) при этом переносится почти без изменений.

Подробности: [docs/01-map-analysis.md](docs/01-map-analysis.md).

## Принцип работы

Карта остаётся **единственным источником правды**. Ничего не редактируется вручную:
все данные движка порождаются из `.w3x` воспроизводимым конвейером.

```
WFWA.w3x ──► extract_map.py ──► сырые файлы WC3 ──► export_data.py ──► JSON для движка
                                       └──────────► (далее) конвертеры MDX→glTF, BLP→PNG
```

Пересобрать данные после выхода новой версии карты — одна команда.

## Быстрый старт

На Windows всё это делает один файл: положите карту рядом с `play.bat`
и запустите его — он проверит окружение, соберёт данные, поставит зависимости
и откроет игру в браузере.

```bash
# Весь конвейер одной командой: распаковка, анализ, конвертация,
# публикация в оба клиента
python3 build.py путь/к/WFWA_v0.9.9q.w3x

# Затем — веб-клиент: меню, лобби на 12 слотов, матч
cd web && npm install && npm run dev     # http://localhost:5173

# …или нативная сборка: открыть каталог godot/ в Godot 4.3+

# Рантайм: подтянуть скрипты и таблицы Warcraft III (~4 МБ, в git не хранятся),
# разрешить объекты карты против стоковых прототипов и прогнать скрипт карты
python3 tools/fetch_war3_data.py
python3 tools/export_stock.py build/war3 build/data
node engine/cli/run-jass.ts build/extracted/war3map.j

# Запуск игры: инициализация, ход времени, бой
node engine/cli/run-game.ts --seconds 300
node engine/cli/run-battle.ts --list        # стенд для проверки баланса

# Бой в браузере: собрать страницу и открыть её (файл в git не хранится)
python3 tools/fetch_war3_art.py
python3 tools/make_battle_demo.py            # -> build/battle.html

# Проверки — установка не требуется, Node исполняет TypeScript сам
node engine/test/smoke.ts                    # 74 проверки рантайма
node web/test/smoke.ts                       # 34 проверки лобби, матча, ботов
```

Python 3.8+ и Node 23.6+ (с этой версии TypeScript исполняется напрямую).
Конвертация ассетов требует Pillow (`pip install Pillow`); всё остальное
работает на стандартной библиотеке. Есть `Makefile` с теми же целями, если
у вас есть `make` — но на Windows он не запустится, там только `build.py`.

## Что уже работает

| Компонент | Статус | Описание |
|---|---|---|
| `tools/wc3/mpq.py` | ✅ | Чтение архивов MPQ: хеш/блок-таблицы, шифрование, секторы |
| `tools/wc3/explode.py` | ✅ | Распаковка PKWARE DCL (нет в стандартной библиотеке Python) |
| `tools/wc3/w3x.py` | ✅ | Парсеры `w3i`, `w3e`, `doo`, `w3u/w3t/w3a/w3b/w3d/w3h/w3q`, `wts`, `imp` |
| `tools/extract_map.py` | ✅ | Распаковка карты целиком — 449/449 файлов без ошибок |
| `tools/analyze_map.py` | ✅ | Отчёт: территория, объекты, скрипт, вес ассетов |
| `tools/analyze_jass.py` | ✅ | Определение требуемой поверхности API движка |
| `tools/export_data.py` | ✅ | Экспорт всех игровых таблиц в JSON |
| `tools/wc3/blp.py` | ✅ | Декодер текстур BLP1 (режимы JPEG и палитровый) |
| `tools/wc3/mdx.py` | ✅ | Парсер моделей MDX v800 |
| `tools/wc3/gltf.py` | ✅ | Запись glTF 2.0 / GLB |
| `tools/convert_textures.py` | ✅ | 156/156 текстур, 13.3 МБ → 3.7 МБ (−72%) |
| `tools/convert_models.py` | ✅ | 262/262 модели в glTF, 223 365 треугольников |
| `tools/preview_glb.py` | ✅ | Программный рендер модели в PNG для проверки |
| `tools/preview_terrain.py` | ✅ | Рендер ландшафта для сверки с миникартой |
| `build.py` | ✅ | Весь конвейер и публикация в оба клиента |
| `web/` | ✅ | Клиент на TypeScript + WebGPU: меню, лобби, матч, HUD |
| `web/src/shell/` | ✅ | Оболочка игры: экраны, **лобби на 12 слотов с ботами** |
| `web/src/worker/` | ✅ | Симуляция в отдельном потоке, снимки состояния в рендер |
| `web/src/hud/` | ✅ | Ресурсы, часы, выделение, приказы, миникарта, счёт |
| `engine/ai/` | ✅ | Водитель армий компьютерных игроков |
| `godot/` | ✅ | Проект Godot 4 — нативная сборка |
| `tools/wc3/slk.py` | ✅ | Чтение таблиц SYLK — стоковый баланс Warcraft III |
| `tools/fetch_war3_data.py` | ✅ | Загрузка `common.j`, `Blizzard.j` и таблиц SLK (~4 МБ, в git не хранятся) |
| `tools/export_stock.py` | ✅ | Разрешение объектов карты против стоковых прототипов — 1 320/1 320 |
| `tools/analyze_assets.py` | ✅ | Замер зависимости от War3.mpq |
| `tools/fetch_war3_art.py` | ✅ | Загрузка нужной графики — 919/922 ссылки, 39 МБ, вне git |
| `tools/make_battle_demo.py` | ✅ | Сборка браузерного стенда боя из локальных данных |
| `engine/jass/` | ✅ | Лексер, парсер, интерпретатор с приостанавливаемыми потоками |
| `engine/sim/` | ◐ | Часы 32 Гц, таймеры, события, **бой на характеристиках карты** |
| Движок: поиск пути | ⬜ | Юниты ходят по прямой, `war3map.wpm` не читается |
| Движок: ввод и приказы | ✅ | Выделение рамкой, приказы, камера, горячие клавиши |
| Движок: рендер моделей | ⬜ | Геометрия пока кубы, ассеты сконвертированы |
| Сетевая игра | ⬜ | Этап B: lockstep поверх готовой границы «команды — снимки» |

## Структура

```
build.py        весь конвейер одной командой
tools/          извлечение и конвертация (Python)
  wc3/          библиотека форматов Warcraft III: mpq, blp, mdx, w3x, gltf
engine/         рантайм (TypeScript)
  jass/         лексер, парсер, интерпретатор, хендлы, функции контракта
  sim/          часы 32 Гц, таймеры, события
  ai/           водитель армий ботов
  cli/          прогон скрипта карты, запуск игры, отчёты
  demo/         шаблон браузерного стенда боя (данные подставляются сборкой)
  test/         регрессионный тест — 74 проверки
web/            клиент на TypeScript + WebGPU (основной)
  src/shell/    экраны: меню, лобби, матч, результаты
  src/game/     модель матча и контракт с симуляцией
  src/worker/   симуляция в отдельном потоке
  src/hud/      внутриигровой интерфейс и миникарта
  test/         регрессионный тест оболочки — 34 проверки
godot/          проект Godot 4 (нативная сборка)
docs/           анализ, архитектура, дорожная карта
  data/         машиночитаемые отчёты анализа
build/          генерируемые артефакты (в git не хранятся)
```

Оба клиента читают **один и тот же** результат конвейера и никогда не
разбирают `.w3x` сами — иначе они неизбежно разойдутся по содержимому.

## Исходная карта

`.w3x` намеренно **не** лежит в репозитории: 25.6 МБ бинарника в истории git
никому не помогут. Варианты хранения — Git LFS или GitHub Releases.
См. [docs/03-roadmap.md](docs/03-roadmap.md).

## Документация

- [01 — Анализ карты](docs/01-map-analysis.md) — что внутри и почему 1.26 не тянет
- [02 — Архитектура движка](docs/02-architecture.md) — как устроен целевой рантайм
- [03 — Дорожная карта](docs/03-roadmap.md) — этапы работ
- [04 — Два клиента](docs/04-frontends.md) — web и Godot, что общее и что нет
- [05 — Запуск без Warcraft III](docs/05-standalone.md) — три барьера до самостоятельной игры
- [06 — Направление продукта](docs/06-product.md) — одиночная игра, онлайн, независимость от WC3
- [07 — Внешний интерфейс](docs/07-interface.md) — экраны, лобби на 12 слотов, боты, управление

---

Автор исходной карты: **[TZ.Ent]AeNeMeR / TrioZ** ([trioz.ru](http://trioz.ru)).
