{
  "name": "weldaran-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "WebGPU frontend for the War for WeldAran engine",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.40",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
